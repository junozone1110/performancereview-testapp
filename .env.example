# Database (SQLite)
# Note: Path is relative to prisma/ directory
DATABASE_URL="file:../data/performance_review.db"

# NextAuth
AUTH_SECRET="your-secret-key-here"
AUTH_TRUST_HOST=true

# ===== Docker環境用 =====
# docker-compose使用時は以下を設定

# Docker環境のDATABASE_URL（docker-compose.ymlで自動設定されるため通常は不要）
# DATABASE_URL="file:/app/data/performance_review.db"
